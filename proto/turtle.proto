syntax = "proto3";
package turtleproto;

message TurtleCommand {
    enum Direction{
        STOP = 0; //default no movement
        LEFT = 1;
        RIGHT = 2;
        FORWARD = 3;
        BACKWARD = 4;
    };
    Direction direction = 1;
}